import Layout from '@/components/Layout'
import { Code, Key, Zap, BookOpen } from 'lucide-react'

const endpoints = [
{ method: "GET", path: "/v1/articles", description: "List & search articles with filters" },
{ method: "GET", path: "/v1/articles/:id", description: "Retrieve a single article by ID" },
{ method: "GET", path: "/v1/trending", description: "Get currently trending articles" },
{ method: "GET", path: "/v1/categories", description: "List all available categories" },
{ method: "GET", path: "/v1/sources", description: "List all verified publishers" },
{ method: "POST", path: "/v1/bookmarks", description: "Save an article to bookmarks" },
];

const ApiDocs = () => {
return (

<Layout>
{/* Hero */}
<section className="border-b border-border bg-primary py-16 text-primary-foreground">
<div className="container text-center">
<h1 className="mb-4 font-serif text-4xl font-bold md:text-5xl">
API Documentation
</h1>
<p className="mx-auto max-w-2xl text-lg text-primary-foreground/80">
Integrate NewScout's aggregated news into your own applications.
</p>
</div>
</section>

      <section className="container py-10">
        <div className="mx-auto max-w-4xl space-y-10">
          {/* Quick start cards */}
          <div className="grid gap-4 sm:grid-cols-3">
            {[
              { icon: Key, title: "Authentication", desc: "Use API keys from your dashboard to authenticate requests." },
              { icon: Zap, title: "Rate Limits", desc: "Free: 100 req/day. Pro: 10k req/mo. Team: custom." },
              { icon: BookOpen, title: "Response Format", desc: "All endpoints return JSON with pagination metadata." },
            ].map((c) => (
              <div key={c.title} className="rounded-xl border border-border bg-card p-5">
                <c.icon className="mb-3 h-6 w-6 text-primary" />
                <h3 className="mb-1 font-semibold text-foreground">{c.title}</h3>
                <p className="text-sm text-muted-foreground">{c.desc}</p>
              </div>
            ))}
          </div>

          {/* Base URL */}
          <div>
            <h2 className="mb-3 font-serif text-xl font-bold text-foreground">Base URL</h2>
            <div className="rounded-lg border border-border bg-muted px-4 py-3 font-mono text-sm text-foreground">
              https://api.newscout.app
            </div>
          </div>

          {/* Endpoints */}
          <div>
            <h2 className="mb-4 font-serif text-xl font-bold text-foreground">Endpoints</h2>
            <div className="overflow-hidden rounded-xl border border-border">
              <table className="w-full text-sm">
                <thead>
                  <tr className="border-b border-border bg-surface text-left">
                    <th className="px-4 py-3 font-semibold text-foreground">Method</th>
                    <th className="px-4 py-3 font-semibold text-foreground">Path</th>
                    <th className="px-4 py-3 font-semibold text-foreground">Description</th>
                  </tr>
                </thead>
                <tbody>
                  {endpoints.map((ep, i) => (
                    <tr key={i} className="border-b border-border last:border-0">
                      <td className="px-4 py-3">
                        <span className="rounded bg-primary/10 px-2 py-0.5 font-mono text-xs font-semibold text-primary">
                          {ep.method}
                        </span>
                      </td>
                      <td className="px-4 py-3 font-mono text-foreground">{ep.path}</td>
                      <td className="px-4 py-3 text-muted-foreground">{ep.description}</td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>
          </div>

          {/* Example */}
          <div>
            <h2 className="mb-3 font-serif text-xl font-bold text-foreground">Example Request</h2>
            <div className="overflow-x-auto rounded-lg border border-border bg-muted p-4">
              <pre className="font-mono text-sm text-foreground">

{`curl -H "Authorization: Bearer YOUR_API_KEY" \\
     "https://api.newscout.app/v1/articles?category=technology&limit=10"`}

</pre>
</div>
</div>

          {/* Code icon footer */}
          <div className="flex items-center justify-center gap-2 text-sm text-muted-foreground">
            <Code className="h-4 w-4" />
            <span>Full SDK & client libraries coming soon.</span>
          </div>
        </div>
      </section>
    </Layout>

);
};

export default ApiDocsContainer
